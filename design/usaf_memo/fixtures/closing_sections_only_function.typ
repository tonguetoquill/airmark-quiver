// The bodyless memorandum in the form that never hands a closing section to the
// rebuild — the baseline `closing_sections_only.typ` has to match.

#import "closing_sections.typ": *

#show: addressing
#mainmatter([])
#closing()
